<%= form_for(game_review, url: game_reviews_path(@review.game_id)) do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :body %>
<%= f.text_area :body %>

<%= f.submit "Post review" %>
<%end%>